# H3 输出格式修复验证

日期：2026-09-14。

## 修复范围

- 逐字段生成，由程序固定三段/六段标题与顺序。
- 规范化主体标签前的 Markdown 列表/加粗，不跳过引用与主体校验。
- 按模式、字段使用 Skill 规范章节，排除整篇案例和代码块示例。
- 对从 0 秒起、连续且不超时的中文区间分镜，固定切镜时间；明确标注的台词、字幕直接保留原文。
- 对其他引号原文使用占位保护与回填，遗漏时校验失败。
- 校验失败最多自动修正一次；不返回不合格结果。

## 验证结果

- 25 项离线测试通过。
- 5090 上真实三张参考图任务：六段校验通过，切镜为 0、3、6、10、13 秒，两句中文台词和字幕原文保留。
- 5090 上真实无参考固定镜头任务：三段校验通过。
- 已安装节点的 ComfyUI API 冒烟测试成功：LoadImage × 3 → H3LocalSkillOptimizer → H3LocalPromptPreview。
- API 任务 ID：0e4a575b-f3b9-4239-8c10-c408f93807a1，status=success，约 19.3 秒，峰值 CUDA allocated 约 8.31 GiB。
- 未执行下游完整 H3 视频生成；未验证 RunningHub。

## 仍有的限制

结构和原文校验不能替代语义审稿。实测中 7B 模型仍可能改变背景、动作、摘要或声音细节。请预览优化结果再生成视频。显存数据只对应上述优化输入，不代表完整 H3 工作流的显存需求。
